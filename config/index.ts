import { Config } from '../typings/global'

/**
 * 构建配置
 */
const config: Config = {
  /**
   * 是否使用 antd
   */
  antd: true,
  /**
   * 是否使用 rematch 作为状态管理
   */
  rematch: true,
  /**
   * 是否使用 Reactoron
   */
  Reactotron: false
}

export default config
